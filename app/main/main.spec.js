'use strict';

describe('toDoApp.main module', function() {

  beforeEach(module('toDoApp.main'));

  describe('main controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var mainCtrl = $controller('mainCtrl');
      expect(mainCtrl).toBeDefined();
    }));

  });
});