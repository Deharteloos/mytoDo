'use strict';

describe('toDoApp.user module', function() {

  beforeEach(module('toDoApp.user'));

  describe('user controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var userCtrl = $controller('userCtrl');
      expect(userCtrl).toBeDefined();
    }));

  });
});